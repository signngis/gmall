<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.gmall0228.cart.Mapper.CartInfoMapper">
    <delete id="deleteCheckedCart">
        delete from cart_info where user_id = #{userId} and id in (${ids})
    </delete>
</mapper>
